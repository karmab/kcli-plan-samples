127.0.0.1   localhost localhost.localdomain localhost4 localhost4.localdomain4
::1         localhost localhost.localdomain localhost6 localhost6.localdomain6
{{ haproxy_ip }} api.{{ cluster }}.{{ domain }}
{{ haproxy_ip }} api-int.{{ cluster }}.{{ domain }}
{{ haproxy_ip }} ns1.{{ cluster }}.{{ domain }}
{% for num in range(0, masters) %}
{{ masters_ips[num] }} {{ cluster }}-master-{{ num }} {{ cluster }}-master-{{ num }}.{{ cluster }}.{{ domain }} etcd-{{ num }}.{{ cluster }}.{{ domain }}
{% endfor %}
{% if workers > 0 %}
{% for num in range(0, workers) %}
{{ workers_ips[num] }} {{ cluster }}-worker-{{ num }} {{ cluster }}-worker-{{ num }}.{{ cluster }}.{{ domain }}
{% endfor %}
{% endif %}
